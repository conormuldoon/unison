import switch_list_items


if __name__ == "__main__":
    matchlist = ["driverClassName", "hibernate.dialect",
                 "datasource.url", "hibernate.ddl", "spatial.regex"]
    switch_list_items.switchitems(matchlist)
